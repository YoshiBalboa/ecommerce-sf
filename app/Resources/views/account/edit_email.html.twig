{% extends 'base.html.twig' %}

{% block head_title %}{{ head_title }}{% endblock %}

{% block body %}
	<div class="page-header">
		<h1>{{ h1_title }}</h1>
	</div>

	<p>{{ 'view.your-current-email'|trans({'%email%': current_email})|raw }}<p>

	{{ form_start(email_form) }}
	{{ form_widget(email_form) }}
	{{ form_end(email_form) }}
{% endblock %}
